#to download ansible we first have to install pip

#run following command to install pip 
curl https://bootstrap.pypa.io/get-pip.py -o get-pip.py
python3 get-pip.py --user


#after you run both those commads you can check if it succefully downloaded by running
python3 --version
#if it gives a version that means it succesfully downloaded

#now that we pip we can download ansible
#run following command to download ansible
pip install ansible

#wait for it download and install
#done
